info:
  name: Buscar usuário com ID inválido
  type: http
  seq: 3
  tags:
    - aula-04

http:
  method: GET
  url: "{{baseUrl}}/users/abc"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Rejeita ID de usuário inválido", function() {
          expect(res.status).to.equal(400);
          expect(res.body.success).to.equal(false);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
